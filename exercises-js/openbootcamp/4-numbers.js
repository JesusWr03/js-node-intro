const myHeightCm = 183;
const myHeightMt = 1.83;

const myWeight = 70.3;

const roundedHeight = Math.ceil(myHeightMt)
const roundedWeight = Math.floor(myWeight)

const maxValueJs = (Number.MAX_VALUE + 1) === Number.MAX_VALUE;