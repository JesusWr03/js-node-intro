const myHeightCm = 183;
const myHeightMt = 1.83;

const myWeight = 70.3;

const roundedHeight = myHeightMt.toFixed(0)
const roundedWeight = myWeight.toFixed(0)

const maxValueJs = (Number.MAX_VALUE + 1) === Number.MAX_VALUE;